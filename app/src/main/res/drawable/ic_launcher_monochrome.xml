<?xml version="1.0" encoding="utf-8"?>
<!--
  Themed-icon layer. Aliases the foreground glyph rather than duplicating its paths, so
  the two cannot drift apart; the system supplies the tint. The evenOdd hole in the
  display means the cut-out reads correctly once tinted.
-->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@drawable/ic_launcher_abl100we" />
